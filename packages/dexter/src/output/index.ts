/**
 * Structured output — polymorphic Node trees.
 *
 * Renders to CLI (human), JSON, XML, or Markdown.
 * Used by meta framework for all command output.
 */

// TODO: Extract from asombro/platform/lib/dexter/src/output/
export {}
