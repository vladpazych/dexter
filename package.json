{
  "name": "@repo/root",
  "private": true,
  "workspaces": [
    "packages/*",
    "meta/config"
  ],
  "devDependencies": {
    "@repo/meta-config": "workspace:*",
    "@types/bun": "^1",
    "eslint": "^9.22.0"
  },
  "scripts": {
    "bump": "bun run scripts/release.ts",
    "lint": "eslint ."
  }
}
